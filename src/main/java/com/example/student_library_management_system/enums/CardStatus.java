package com.example.student_library_management_system.enums;

public enum CardStatus {
    //enum is a pre-defined data type, which contains constant values for specific fields

    ACTIVE,
    INACTIVE,
    BLOCKED,
    EXPIRED

}
